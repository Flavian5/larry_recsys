# Copy to .env and set values. .env is git-ignored; do not commit secrets.

# Environment: local | composer-dev | composer-prod
RPG_ENV=local

# GCP project (for Composer / GCS)
RPG_GCP_PROJECT=your-gcp-project

# GCS bucket names (base URIs become gs://<bucket>)
RPG_GCS_BUCKET_RAW=your-bucket-raw
RPG_GCS_BUCKET_SILVER=your-bucket-silver
RPG_GCS_BUCKET_GOLD=your-bucket-gold

# Upstream dataset sources (leave empty to use DAG default local paths)
# Overture: base URL used with RPG_OVERTURE_RELEASE_DATE to build the Places URL
RPG_OVERTURE_PLACES_BASE_URL=https://overturemaps-us-west-2.s3.amazonaws.com
# Full Overture Places URI (overrides base+release when set)
# RPG_OVERTURE_PLACES_URI=
# Release date when using base URL (e.g. 2024-03-12)
# RPG_OVERTURE_RELEASE_DATE=
# OSM extract URL or path
# RPG_OSM_EXTRACT_URI=

# Local output: parquet (default) | text (plain text for inspecting silver/gold)
RPG_LOCAL_OUTPUT_FORMAT=parquet

# Optional: sync local gold output to GCS after the DAG runs
# RPG_ENABLE_LOCAL_GCS_SYNC=false
# RPG_GCS_GOLD_URI=gs://your-bucket-gold/venues.parquet
